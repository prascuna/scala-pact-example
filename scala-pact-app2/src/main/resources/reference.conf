app {
  scheme = "http"
  host = "localhost"
  port = 9092
}

service{
  forwardMethod = "POST"
  forwardURI = "http://localhost:9093/data"
}

sampledb = {
  url = "jdbc:mysql://localhost:3306/sampledb?autoReconnect=true&useSSL=false"
  user = dbuser
  password = dbpassword
}